# OpenAI API key — required for sentiment_scorer.py and anomaly_agent.py
OPENAI_API_KEY=sk-...

# Google Places API key — required for sort_reviews_by_date.py (Places fetch step)
# Can be skipped with --skip-fetch if places_json/ files already exist
GOOGLE_MAPS_API_KEY=AIza...
